{
  "version": 2,
  "routes": [
    { "src": "/api/auth/(.*)", "dest": "/api/auth/$1" },
    { "src": "/api/tokens/(.*)", "dest": "/api/tokens/$1" },
    { "src": "/api/(.*)", "dest": "/api/proxy?path=$1" },
    { "handle": "filesystem" },
    { "src": "/(.*)", "dest": "/index.html" }
  ]
}
